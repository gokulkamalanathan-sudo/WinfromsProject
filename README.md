This project automates GitHub account creation using Selenium WebDriver inside a Windows Forms application.

It takes an email from the user, generates a unique tagged email format, fills GitHub’s sign-up form, and submits it automatically.
